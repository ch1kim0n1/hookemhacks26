# ZK circuits (placeholders)

- **ScanAttestation** — attests scanner output binding (adapt from your circom sources).
- **DefenseUpdateCorrectness** — proves learning-loop policy transition.

Wire bytecode verifiers to `MockGroth16Verifier` in tests, then swap for production verifier contracts on Base.
