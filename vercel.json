{
  "framework": null,
  "installCommand": "uv pip install --system -r requirements.txt && cd dashboard && npm install",
  "buildCommand": "cd dashboard && npm run build && mkdir -p ../public && cp -r dist/* ../public/",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index.py" }
  ]
}